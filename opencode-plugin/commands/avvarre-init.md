---
description: Scaffold .avvarre/ project memory structure for this workspace
---
Use the avvarre MCP server to scaffold the project memory directory for this workspace.

Call the `scaffold_avvarre` tool from the `avvarre` MCP server with `workspaceRoot` set to the absolute path of the current workspace root directory.

After scaffolding, briefly summarize what files were created, what was skipped (already existed), and remind the user to fill in `context.md` and `conventions.md` with project-specific details.
